#include <algorithm>
#include <iostream>
#include <vector>
#include<string>
using namespace std;
#include<bits/stdc++.h>
void func(int n){
	//print karpakar number
	int square =n*n;
	string a=to_string(square);
	bool found=false;
	for(int i=1;i<a.size();i++){
		int left=stoi(a.substr(0,i));
		int right=stoi(a.substr(i));
		if(right==0){
			continue;
		}
		int sum=left+right;
		if(sum==n){
			found=true;
			break;
		}
	}
	if(found){
		cout<<"Kaprekar";
	}
	else{
		cout<<"Not Kaprekar";
	}
	}
	

int main() {
	func(45);
	return 0;
}
